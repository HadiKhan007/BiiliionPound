const BASE_URL = 'https://api.resihop.com/';

const ENDPOINTS = {
  REGISTER: 'register',
  LOGIN: 'auth/local',
  LOGOUT: 'logout',
  ACCESS_TOKEN: 'get_access_token',
};

export {BASE_URL, ENDPOINTS};
